# Bridge runtime pins validated with the repository's current integration
# environment. API and bridge share the same protocol files but remain
# separately deployable processes.
boto3==1.43.89
jsonschema==4.10.3
pika==1.4.4
psycopg2-binary==2.9.10
pydantic==2.13.4
pydantic-settings==2.15.0
python-docx==1.2.0
PyYAML==6.0.2
